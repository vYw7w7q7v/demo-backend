spring:
  application:
    name: EventEase-backend
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    platform: postgres
    driverClassName: org.postgresql.Driver
    username: postgres
    password: 1234
    flyway:
      baseline-on-migrate: true
